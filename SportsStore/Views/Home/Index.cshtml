@model IQueryable<Product>

@foreach (var Item in Model)
{
    <div>
        <h3>
            @Item.Name

        </h3>
        @Item.Description
        <h4>
            @Item.Price.ToString("c")
        </h4>
    </div>
}
<h4>Welcome to SportsStore</h4>